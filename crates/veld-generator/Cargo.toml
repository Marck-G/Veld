[package]
name = "veld-generator"
version = "0.1.0"
edition = "2024"

[dependencies]
veld-error = { version = "0.1.0", path = "../veld-error" }
semver = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
